%h2=t('.title')
%p=t('.intro')
= semantic_form_for @user do |f|
  = f.semantic_errors
  = f.inputs do
    = f.input :email
    = f.input :password
    = f.input :password_confirmation

  = f.buttons do
    = f.commit_button :label => t(:register)
