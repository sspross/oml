%h2=t('.title')
%p=t('.intro')
= semantic_form_for @user_session do |f|
  = f.inputs do
    = f.input :email, :label => User.human_attribute_name(:email)
    = f.input :password, :label => User.human_attribute_name(:password)

  = f.buttons do
    = f.commit_button :label => t(:login)
