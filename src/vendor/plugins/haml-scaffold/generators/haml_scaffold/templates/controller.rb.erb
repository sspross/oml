class <%= controller_class_name %>Controller < ResourceController::Base

  <%= file_name.pluralize.upcase %>_PER_PAGE = 5

  private
  
    def collection
      @collection ||= <%= class_name %>.paginate(:page => params[:page], :per_page => <%= file_name.pluralize.upcase %>_PER_PAGE)
    end

end